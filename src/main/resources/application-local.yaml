server:
  port: 8082
  error:
    include-message: ALWAYS
    include-binding-errors: ALWAYS
    include-stacktrace: ALWAYS
    include-exception: false

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/em_wallet_db
    username: em_micro_db_user
    password: Lastyear1$
  jpa:
    show-sql: true
